function Try(){
    const tryVar = "this is a variable";
    return(
        <div>this is a component for try --- imp thing in function/component name should be in capital -----{tryVar} </div>
    )
}

export default Try